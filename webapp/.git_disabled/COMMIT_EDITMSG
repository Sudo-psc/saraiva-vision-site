fix: Corrigir URLs da API WordPress em produção

- Atualizar .env.production com URLs corretas da API
- Mudar de localhost:8083 para https://saraivavision.com.br
- Rebuild completo com configurações corretas
- Deploy da nova versão para produção

Problema: Frontend estava mostrando posts de fallback porque tentava
conectar em localhost:8083 em vez da URL pública da API.

Solução: Configurar todas as variáveis VITE_WORDPRESS_* com URLs públicas
corretas em produção.
