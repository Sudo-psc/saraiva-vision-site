# GitHub Webhook Receiver Configuration
#
# Copy this file to .env.webhook and configure your values
# DO NOT commit .env.webhook to git!

# Webhook server port (internal, behind Nginx reverse proxy)
WEBHOOK_PORT=9000

# GitHub webhook secret (generate a strong random string)
# This is configured in GitHub repository settings -> Webhooks
# Generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=your_secret_here

# GitHub Personal Access Token (for API access if needed)
# Scopes required: repo, workflow
GITHUB_PAT=your_github_pat_here

# Node environment
NODE_ENV=production
