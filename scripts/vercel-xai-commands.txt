# Comandos Diretos para Configurar xAI no Vercel

## 🚀 Configuração Rápida (Cole os comandos abaixo)

# 1. Instalar Vercel CLI (se necessário)
npm install -g vercel

# 2. Login no Vercel
vercel login

# 3. Configurar XAI_API_KEY (SUBSTITUA pela sua API key real)
echo "xai-your-actual-api-key-here" | vercel env add XAI_API_KEY production
echo "xai-your-actual-api-key-here" | vercel env add XAI_API_KEY preview  
echo "xai-your-actual-api-key-here" | vercel env add XAI_API_KEY development

# 4. Configurar XAI_MODEL
echo "grok-2-1212" | vercel env add XAI_MODEL production
echo "grok-2-1212" | vercel env add XAI_MODEL preview
echo "grok-2-1212" | vercel env add XAI_MODEL development

# 5. Configurar XAI_MAX_TOKENS
echo "8192" | vercel env add XAI_MAX_TOKENS production
echo "8192" | vercel env add XAI_MAX_TOKENS preview
echo "8192" | vercel env add XAI_MAX_TOKENS development

# 6. Configurar XAI_TEMPERATURE
echo "0.1" | vercel env add XAI_TEMPERATURE production
echo "0.1" | vercel env add XAI_TEMPERATURE preview
echo "0.1" | vercel env add XAI_TEMPERATURE development

# 7. Verificar configuração
vercel env ls | grep XAI

# 8. Deploy
vercel --prod

## 🔧 Comandos de Verificação

# Listar todas as variáveis
vercel env ls

# Testar chatbot em produção
curl -X POST https://saraivavision.com.br/api/chatbot \
  -H "Content-Type: application/json" \
  -d '{"message": "Olá, teste do xAI"}'

# Ver logs em tempo real
vercel logs --follow

## 🗑️ Comandos para Remover (se necessário)

# Remover variáveis xAI
vercel env rm XAI_API_KEY production
vercel env rm XAI_API_KEY preview
vercel env rm XAI_API_KEY development

vercel env rm XAI_MODEL production
vercel env rm XAI_MODEL preview
vercel env rm XAI_MODEL development

vercel env rm XAI_MAX_TOKENS production
vercel env rm XAI_MAX_TOKENS preview
vercel env rm XAI_MAX_TOKENS development

vercel env rm XAI_TEMPERATURE production
vercel env rm XAI_TEMPERATURE preview
vercel env rm XAI_TEMPERATURE development

## 📋 Checklist de Configuração

□ Vercel CLI instalado
□ Login no Vercel realizado
□ API key do xAI obtida em console.x.ai
□ XAI_API_KEY configurada para todos os ambientes
□ XAI_MODEL configurada (grok-2-1212)
□ XAI_MAX_TOKENS configurada (8192)
□ XAI_TEMPERATURE configurada (0.1)
□ Deploy realizado
□ Teste do chatbot funcionando
□ Logs verificados

## 💡 Notas Importantes

1. SUBSTITUA "xai-your-actual-api-key-here" pela sua API key real do xAI
2. Obtenha sua API key em: https://console.x.ai
3. O sistema tem fallback automático se a API key não estiver configurada
4. Monitore os logs para verificar se está funcionando: vercel logs --follow