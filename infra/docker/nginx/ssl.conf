# SSL configuration for production deployment
# This file is included when SSL certificates are available

# SSL session configuration
ssl_session_cache shared:SSL:10m;
ssl_session_timeout 10m;

# SSL protocols and ciphers (modern configuration)
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
ssl_prefer_server_ciphers off;

# HSTS (HTTPS Strict Transport Security)
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

# OCSP Stapling
ssl_stapling on;
ssl_stapling_verify on;

# SSL certificate paths (to be mounted as volumes or secrets)
# ssl_certificate /etc/nginx/ssl/fullchain.pem;
# ssl_certificate_key /etc/nginx/ssl/privkey.pem;
# ssl_trusted_certificate /etc/nginx/ssl/chain.pem;

# Redirect HTTP to HTTPS (uncomment when using SSL)
# server {
#     listen 80;
#     server_name saraivavision.com.br www.saraivavision.com.br;
#     return 301 https://$server_name$request_uri;
# }