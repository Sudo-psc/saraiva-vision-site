# Dev uses same-origin API via Vite proxy to avoid CSP/CORS issues
# Keep requests relative so the browser sees them as same-origin.
VITE_WORDPRESS_API_URL=/wp-json/wp/v2

# Note: Do NOT set absolute localhost URLs here in dev.
# The Vite proxy in vite.config.js forwards /wp-json/* to http://localhost:8083
VITE_WORDPRESS_ADMIN_URL=http://localhost:8080/wp-admin
VITE_BLOG_UNDER_CONSTRUCTION="0"