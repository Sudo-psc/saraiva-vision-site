// Simple test to verify the useGlassMorphism hook exists
import { describe, it, expect } from 'vitest';

describe('Test Runner', () => {
    it('should be able to run tests', () => {
        expect(true).toBe(true);
    });
});